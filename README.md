# ERP 題庫刷題網頁

這是一個可部署到 GitHub Pages 的純靜態刷題網頁。

## 檔案

- `index.html`：網頁入口
- `app.css`：版面樣式
- `app.js`：題庫解析、隨機出題、作答判斷
- `ERP_text.txt`：題庫資料來源

## 使用方式

把以上檔案放在同一層，部署到 GitHub Pages 後即可使用。

每次開始或重新開始時，題目順序會重新隨機排列，每題選項也會隨機排列。選錯會立即顯示選到的錯誤選項與正確選項，全部作答完會公布分數。
